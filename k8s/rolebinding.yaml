apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: matchmaking-rolebinding
  namespace: game-backend  # Adjust namespace as needed
subjects:
- kind: ServiceAccount
  name: matchmaking-sa
  namespace: game-backend  # Adjust namespace as needed
roleRef:
  kind: Role
  name: matchmaking-role
  apiGroup: rbac.authorization.k8s.io